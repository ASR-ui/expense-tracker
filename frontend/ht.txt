<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Expense Tracker</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,400;0,9..144,600;0,9..144,700;1,9..144,500&family=IBM+Plex+Mono:wght@400;500;600&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root{
    --paper: #ece4d3;
    --ink: #22281f;
    --ink-soft: #4a5142;
    --green: #2f5233;
    --green-deep: #203a24;
    --brass: #a9814a;
    --brass-light: #cba86a;
    --red: #a13d2b;
    --rule: #c7bb9c;
    --rule-strong: #a89a72;
    --card-bg: #f4eee0;
    --paper-white: #fffdf8;
    --tan: #e2d8c1;
    --font-display: "Fraunces", serif;
    --font-body: "Inter", sans-serif;
    --font-mono: "IBM Plex Mono", monospace;
  }
  *{ box-sizing: border-box; }
  [hidden]{ display: none !important; }
  body{
    margin:0;
    background:
      repeating-linear-gradient(180deg, transparent 0 38px, rgba(169,129,74,0.07) 38px 39px),
      var(--paper);
    color: var(--ink);
    font-family: var(--font-body);
    min-height: 100vh;
  }

  /* ----- ANIMATIONS ----- */
  @keyframes fadeSlideUp {
      from { opacity: 0; transform: translateY(15px); }
      to { opacity: 1; transform: translateY(0); }
  }
  @keyframes scaleYUp {
      from { transform: scaleY(0); }
      to { transform: scaleY(1); }
  }
  @keyframes drawArc {
      from { stroke-dasharray: 0 2000; }
      to { stroke-dasharray: var(--target-dash) var(--target-gap); }
  }
  @keyframes popText {
      from { opacity: 0; transform: translateY(5px); }
      to { opacity: 1; transform: translateY(0); }
  }

  .view:not([hidden]) {
      animation: fadeSlideUp 0.4s ease-out forwards;
  }
  .chart-3d-container {
      animation: fadeSlideUp 0.7s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }

  /* ---------------- Auth screen ---------------- */
  .auth-screen{ min-height: 100vh; display:flex; align-items:center; justify-content:center; padding:24px; }
  .auth-card{
    width:100%; max-width:400px; background:var(--card-bg); border:1px solid var(--rule);
    border-radius:4px; padding:36px 32px 30px; box-shadow:0 4px 0 var(--rule); text-align:center;
    animation: fadeSlideUp 0.6s ease-out forwards; 
  }
  .auth-eyebrow{ font-family:var(--font-mono); letter-spacing:.3em; text-transform:uppercase; font-size:10px; color:var(--brass); margin-bottom:8px; }
  .auth-title{ font-family:var(--font-display); font-size:32px; font-weight:700; color:var(--green-deep); margin:0 0 4px; }
  .auth-title em{ font-style:italic; font-weight:500; color:var(--red); }
  .auth-sub{ font-family:var(--font-display); font-style:italic; font-size:14px; color:var(--ink-soft); margin:0 0 24px; }
  .auth-tabs{ display:flex; border:1px solid var(--rule-strong); border-radius:2px; overflow:hidden; margin-bottom:22px; }
  .auth-tabs button{ flex:1; border:none; background:var(--paper-white); font-family:var(--font-mono); font-size:11px; letter-spacing:.08em; text-transform:uppercase; padding:10px 6px; cursor:pointer; color:var(--ink-soft); transition: background 0.3s, color 0.3s; }
  .auth-tabs button.active{ background:var(--green-deep); color:var(--card-bg); }
  .auth-form{ display:flex; flex-direction:column; gap:14px; text-align:left; }
  .auth-form .name-row{ display:flex; gap:10px; }
  .auth-form .name-row .field{ flex:1; }
  .auth-form label{ font-family:var(--font-mono); font-size:10px; letter-spacing:.12em; text-transform:uppercase; color:var(--ink-soft); display:block; margin-bottom:6px; }
  .auth-form input{ width:100%; font-family:var(--font-body); font-size:14px; padding:10px 11px; border:1px solid var(--rule-strong); border-radius:2px; background:var(--paper-white); color:var(--ink); transition: border-color 0.3s, outline 0.3s; }
  .auth-form input:focus{ outline:2px solid var(--brass); outline-offset:1px; }
  .field-hint{ font-size:11px; color:var(--ink-soft); margin-top:4px; display:block; }
  .pw-wrap{ position:relative; }
  .pw-wrap input{ padding-right:38px; }
  .pw-toggle{
    position:absolute; right:6px; top:50%; transform:translateY(-50%);
    background:none; border:none; cursor:pointer; padding:4px;
    display:flex; align-items:center; justify-content:center;
    color: var(--ink-soft); transition: color 0.2s;
  }
  .pw-toggle:hover{ color: var(--brass); }
  .pw-toggle svg{ width:17px; height:17px; display:block; }
  .auth-error{ font-family:var(--font-mono); font-size:12px; color:var(--red); min-height:14px; text-align:left; }
  .auth-submit{ background:var(--green-deep); color:var(--card-bg); border:none; border-radius:2px; padding:12px; font-family:var(--font-mono); font-size:12px; letter-spacing:.1em; text-transform:uppercase; cursor:pointer; transition: background 0.2s, transform 0.2s; }
  .auth-submit:hover{ background:var(--green); transform: translateY(-1px); }
  .auth-note{ margin-top:18px; font-size:11px; color:var(--ink-soft); line-height:1.5; }

  /* ---------------- App shell ---------------- */
  .shell{ max-width:1080px; margin:0 auto; padding:20px 20px 80px; }

  .topbar{
    display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:12px;
    padding-bottom:16px; border-bottom:1px solid var(--rule);
  }
  .brand{ display:flex; flex-direction:column; }
  .brand .eyebrow{ font-family:var(--font-mono); letter-spacing:.28em; text-transform:uppercase; font-size:10px; color:var(--brass); }
  .brand h1{ font-family:var(--font-display); font-size:22px; font-weight:700; color:var(--green-deep); margin:2px 0 0; }
  .account{ display:flex; align-items:center; gap:12px; font-family:var(--font-mono); font-size:12px; color:var(--ink-soft); }
  .account .who{ color:var(--green-deep); font-weight:600; }
  .logout-btn{ background:none; border:1px solid var(--rule-strong); border-radius:2px; padding:5px 12px; font-family:var(--font-mono); font-size:11px; letter-spacing:.06em; text-transform:uppercase; cursor:pointer; color:var(--ink-soft); transition: all 0.2s; }
  .logout-btn:hover{ color:var(--red); border-color:var(--red); }

  /* nav tabs */
  .nav-tabs{ display:flex; gap:22px; margin-top:16px; flex-wrap:wrap; }
  .nav-tabs button{
    display:flex; align-items:center; gap:7px;
    background:none; border:none; cursor:pointer;
    font-family:var(--font-mono); font-size:11px; letter-spacing:.1em; text-transform:uppercase;
    color: rgba(34,40,31,0.5); padding:6px 0; transition: color 0.3s;
  }
  .nav-tabs button .dot{ width:5px; height:5px; border-radius:50%; background: rgba(34,40,31,0.2); transition: background 0.3s; }
  .nav-tabs button.active{ color: var(--green-deep); font-weight:600; }
  .nav-tabs button.active .dot{ background: var(--brass-light); }

  .view{ margin-top:28px; }
  h2.view-title{ font-family:var(--font-display); font-size:26px; font-weight:700; color:var(--green-deep); margin:0; line-height:1.1; }
  p.view-sub{ font-family:var(--font-body); font-size:14px; color:var(--ink-soft); margin:4px 0 0; }

  /* generic panel */
  .panel{ background:var(--card-bg); border:1px solid var(--rule); border-radius:4px; padding:18px; }
  .panel-dark{ background:var(--green-deep); border-radius:4px; padding:22px; color:var(--card-bg); }

  /* dashboard cards */
  .card-grid{ display:grid; grid-template-columns:repeat(2,1fr); gap:16px; margin-top:22px; }
  .stat-card{ background:var(--card-bg); border:1px solid var(--rule); border-radius:4px; padding:18px; transition: transform 0.3s ease, box-shadow 0.3s ease; }
  .stat-card:hover { transform: translateY(-4px); box-shadow: 0 6px 12px rgba(0,0,0,0.06); } 
  
  .stat-card .lbl{ font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:var(--ink-soft); }
  .stat-card .val{ font-family:var(--font-mono); font-weight:700; font-size:20px; color:var(--ink); margin-top:8px; }
  .stat-card .val.red{ color:var(--red); }
  .stat-card .note{ font-family:var(--font-body); font-size:11px; color:var(--ink-soft); margin-top:4px; }
  .stat-bar{ height:3px; width:100%; background:rgba(32,58,36,0.1); margin-top:12px; border-radius:2px; overflow:hidden; }
  .stat-bar > div{ height:100%; background:var(--green-deep); transition: width 1s ease-out; }

  /* recent transactions */
  .recent-wrap{ margin-top:32px; background:var(--card-bg); border:1px solid var(--rule); border-radius:4px; overflow:hidden; }
  .recent-head{ display:flex; justify-content:space-between; align-items:center; padding:12px 16px; border-bottom:1px solid var(--rule); }
  .recent-head .lbl{ font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:var(--ink-soft); }
  .recent-head button{ background:none; border:none; cursor:pointer; font-family:var(--font-mono); font-size:10px; text-transform:uppercase; color:var(--brass); }
  .recent-head button:hover{ text-decoration:underline; }
  .recent-row{ display:flex; justify-content:space-between; align-items:center; padding:12px 16px; background:var(--paper-white); border-bottom:1px solid rgba(199,187,156,0.6); gap:10px; transition: background 0.2s; }
  .recent-row:hover { background: var(--card-bg); }
  .recent-row:last-child{ border-bottom:none; }
  .recent-left{ display:flex; align-items:center; gap:12px; min-width:0; }
  .bar-stripe{ width:6px; align-self:stretch; border-radius:3px; flex-shrink:0; }
  .bar-stripe.income{ background:var(--green-deep); }
  .bar-stripe.expense{ background:var(--red); }
  .recent-left .rt-cat{ font-family:var(--font-body); font-size:13px; font-weight:500; color:var(--ink); }
  .recent-left .rt-meta{ font-family:var(--font-mono); font-size:10px; color:var(--ink-soft); margin-top:2px; }
  .recent-amt{ font-family:var(--font-mono); font-size:13px; font-weight:500; white-space:nowrap; }
  .recent-amt.income{ color:var(--green-deep); }
  .recent-amt.expense{ color:var(--red); }
  .empty-note{ text-align:center; padding:32px 16px; font-family:var(--font-display); font-style:italic; color:var(--ink-soft); }

  /* New entry form */
  form.entry-form{ display:grid; grid-template-columns:1fr 1fr; gap:20px; margin-top:20px; }
  .field label{ font-family:var(--font-mono); font-size:10px; letter-spacing:.12em; text-transform:uppercase; color:var(--ink-soft); }
  .field input, .field select, .field textarea{
    margin-top:8px; width:100%; background:var(--paper-white); border:1px solid var(--rule);
    border-radius:3px; padding:10px 12px; font-family:var(--font-mono); font-size:13px; color:var(--ink); outline:none; transition: border-color 0.3s;
  }
  .field textarea{ font-family:var(--font-body); resize:vertical; }
  .field input:focus, .field select:focus, .field textarea:focus{ border-color:var(--brass); }
  .field.full{ grid-column:1/-1; }
  .form-actions{ grid-column:1/-1; display:flex; justify-content:flex-end; align-items:center; gap:14px; margin-top:4px; }
  .form-msg{ font-family:var(--font-mono); font-size:12px; color:var(--red); }
  .submit-btn{ background:var(--green-deep); color:var(--card-bg); border:none; font-family:var(--font-mono); font-size:11px; letter-spacing:.15em; text-transform:uppercase; padding:12px 24px; border-radius:4px; cursor:pointer; transition: background 0.2s, transform 0.2s; }
  .submit-btn:hover{ background:var(--green); transform: translateY(-1px); }

  /* Ledger table */
  .ledger-panel{ background:var(--card-bg); border:1px solid var(--rule); border-radius:4px; overflow:hidden; margin-top:20px; }
  .ledger-head{ padding:16px 20px; border-bottom:1px solid var(--rule); background:var(--green-deep); display:flex; justify-content:space-between; align-items:center; }
  .ledger-head h3{ font-family:var(--font-display); font-size:18px; font-weight:700; color:var(--card-bg); margin:0; }
  .ledger-head p{ font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:rgba(244,238,224,.7); margin:4px 0 0; }
  table.ledger{ width:100%; border-collapse:collapse; text-align:left; }
  table.ledger thead th{ background:var(--tan); font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:var(--ink-soft); padding:10px 16px; }
  table.ledger tbody td{ padding:11px 16px; border-bottom:1px solid rgba(199,187,156,.5); font-size:13px; transition: background 0.2s; }
  table.ledger tbody tr:hover td{ background:rgba(255,255,255,0.4); }
  table.ledger tbody tr:nth-child(even){ background:var(--card-bg); }
  table.ledger tbody tr:nth-child(odd){ background:var(--paper-white); }
  .type-pill{ font-family:var(--font-mono); font-size:9px; text-transform:uppercase; letter-spacing:.1em; padding:3px 8px; border-radius:2px; border:1px solid; }
  .type-pill.income{ background:var(--green-deep); color:var(--card-bg); border-color:var(--green-deep); }
  .type-pill.expense{ background:var(--red); color:var(--card-bg); border-color:var(--red); }
  td.amt.income{ color:var(--green-deep); font-family:var(--font-mono); }
  td.amt.expense{ color:var(--red); font-family:var(--font-mono); }
  td.desc-cell{ max-width:220px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; color:var(--ink-soft); }
  .del-link{ background:none; border:none; cursor:pointer; font-family:var(--font-mono); font-size:10px; text-transform:uppercase; color:var(--red); transition: transform 0.2s;}
  .del-link:hover{ text-decoration:underline; transform: scale(1.05); }

  /* Analysis */
  .analysis-grid{ display:grid; grid-template-columns:1fr 1fr; gap:16px; margin-top:22px; }
  .chart-holder{ height:260px; margin-top:14px; position:relative; }
  .legend-row{ display:flex; flex-wrap:wrap; gap:8px 14px; margin-top:12px; }
  .legend-row span{ font-family:var(--font-mono); font-size:10px; display:flex; align-items:center; gap:6px; color:var(--ink); }
  .legend-row .swatch{ width:8px; height:8px; border-radius:50%; display:inline-block; }
  .legend-bars{ display:flex; gap:16px; margin-top:12px; }
  .legend-bars span{ font-family:var(--font-mono); font-size:10px; display:flex; align-items:center; gap:6px; }
  .legend-bars .swatch{ width:8px; height:8px; display:inline-block; }

  .suggest-panel{ margin-top:24px; }
  .suggest-panel .head{ display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:8px; }
  .suggest-panel h3{ font-family:var(--font-display); font-size:18px; font-weight:700; color:var(--green-deep); margin:0; }
  .placeholder-tag{ font-family:var(--font-mono); font-size:9px; text-transform:uppercase; letter-spacing:.12em; background:var(--tan); border:1px solid var(--rule); padding:3px 8px; border-radius:2px; color:var(--ink-soft); }
  .suggest-intro{ font-family:var(--font-display); font-style:italic; font-size:13px; color:var(--ink-soft); margin:10px 0 0; }
  .suggest-list{ margin-top:16px; display:flex; flex-direction:column; gap:10px; transition: filter 0.5s ease; }
  .suggest-item{ display:flex; gap:12px; background:var(--paper-white); border:1px solid rgba(199,187,156,0.7); border-radius:3px; padding:12px; opacity:.85; transition: transform 0.2s, opacity 0.2s; }
  .suggest-item:hover { opacity: 1; transform: translateX(2px); }
  .suggest-item .tag{ font-family:var(--font-mono); font-size:9px; text-transform:uppercase; background:var(--tan); border:1px solid var(--rule); padding:2px 6px; height:fit-content; }
  .suggest-item p{ font-family:var(--font-body); font-size:12px; color:var(--ink-soft); line-height:1.5; margin:0; }
  .suggest-foot{ font-family:var(--font-mono); font-size:10px; color:rgba(74,81,66,.7); margin-top:14px; }

  /* ----- 3D CSS BAR CHART ----- */
  .chart-3d-container { 
      display: flex; align-items: flex-end; justify-content: center; gap: 80px; 
      height: 150px; margin: 15px 0 35px; padding-bottom: 20px; 
      border-bottom: 1px dashed rgba(199,187,156,0.6); 
  }
  .bar-3d-wrapper { display: flex; flex-direction: column; align-items: center; margin-right: 15px; }
  .bar-3d { 
      position: relative; width: 45px; background: var(--green-deep); 
      border-bottom-left-radius: 2px; border-bottom-right-radius: 2px;
      transition: height 1s cubic-bezier(0.2, 0.8, 0.2, 1); 
  }
  .bar-3d.boost { background: var(--brass-light); }
  
  .bar-3d::before { 
      content: ''; position: absolute; top: -15px; left: 0; width: 100%; height: 15px; 
      background: inherit; transform-origin: bottom left; transform: skewX(-45deg); filter: brightness(1.15); 
  }
  .bar-3d::after { 
      content: ''; position: absolute; top: 0; right: -15px; width: 15px; height: 100%; 
      background: inherit; transform-origin: top left; transform: skewY(-45deg); filter: brightness(0.85); border-bottom-right-radius: 2px;
  }
  .bar-val { font-family: var(--font-mono); font-size: 11px; margin-bottom: 18px; margin-left: 15px; z-index: 2; }
  .bar-lbl { font-family: var(--font-mono); font-size: 11px; font-weight: 600; margin-top: 8px; margin-left: 15px; color: var(--ink-soft); }

  /* Summary */
  .summary-grid{ display:grid; grid-template-columns:2fr 1fr; gap:16px; margin-top:22px; }
  .summary-left{ display:grid; grid-template-columns:1fr 1fr; gap:16px; }
  .summary-card{ background:var(--card-bg); border:1px solid var(--rule); border-radius:4px; padding:22px; transition: transform 0.3s, box-shadow 0.3s;}
  .summary-card:hover { transform: translateY(-4px); box-shadow: 0 6px 12px rgba(0,0,0,0.06); }
  .summary-card .lbl{ font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:var(--ink-soft); }
  .summary-card .val{ font-family:var(--font-mono); font-weight:700; font-size:26px; margin-top:10px; }
  .summary-card .val.red{ color:var(--red); }
  .summary-card .sub{ font-family:var(--font-mono); font-size:10px; text-transform:uppercase; color:var(--ink-soft); margin-top:8px; }
  .savings-banner{ grid-column:1/-1; background:var(--green-deep); border-radius:4px; padding:22px; display:flex; justify-content:space-between; align-items:center; flex-wrap:wrap; gap:12px; transition: transform 0.3s, box-shadow 0.3s; }
  .savings-banner:hover { transform: translateY(-4px); box-shadow: 0 6px 12px rgba(0,0,0,0.1); }
  .savings-banner .lbl{ font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:rgba(244,238,224,.6); }
  .savings-banner .val{ font-family:var(--font-mono); font-weight:700; font-size:28px; color:var(--card-bg); margin-top:8px; }
  .savings-banner .r-italic{ font-family:var(--font-display); font-style:italic; font-size:14px; color:var(--brass-light); text-align:right; }
  .savings-banner .r-sub{ font-family:var(--font-mono); font-size:10px; text-transform:uppercase; color:rgba(244,238,224,.6); margin-top:4px; text-align:right; }

  .gauge-card{ background:var(--card-bg); border:1px solid var(--rule); border-radius:4px; padding:22px; display:flex; flex-direction:column; align-items:center; justify-content:center; text-align:center; }
  .gauge-card .lbl{ font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:var(--ink-soft); }
  .gauge-wrap{ position:relative; width:140px; height:140px; margin-top:18px; }
  .gauge-wrap svg{ width:100%; height:100%; transform:rotate(-90deg); }
  .gauge-center{ position:absolute; inset:0; display:flex; flex-direction:column; align-items:center; justify-content:center; }
  .gauge-center .pct{ font-family:var(--font-mono); font-weight:700; font-size:26px; color:var(--green-deep); }
  .gauge-center .tag{ font-family:var(--font-mono); font-size:9px; text-transform:uppercase; color:var(--ink-soft); }
  .gauge-card .desc{ font-family:var(--font-body); font-size:12px; color:var(--ink-soft); margin-top:14px; line-height:1.4; }

  footer.tagline{ text-align:center; margin-top:48px; }
  footer.tagline p{ font-family:var(--font-mono); font-size:9px; letter-spacing:.25em; text-transform:uppercase; color:rgba(169,129,74,.7); }

  @media (max-width: 760px){
    .card-grid, .analysis-grid, .summary-grid, .summary-left{ grid-template-columns:1fr; }
    form.entry-form{ grid-template-columns:1fr; }
    table.ledger{ font-size:12px; }
    .auth-form .name-row{ flex-direction:column; }
  }
</style>
</head>
<body>

<!-- ================= AUTH SCREEN ================= -->
<div class="auth-screen" id="authScreen">
  <div class="auth-card">
    <div class="auth-eyebrow">Personal Accounts</div>
    <h1 class="auth-title">Welcome to <em>Expense Tracker</em></h1>
    <p class="auth-sub" id="authSubText">Sign in to open your book.</p>

    <div class="auth-tabs">
      <button type="button" data-tab="login" class="active" id="tabLogin">Log In</button>
      <button type="button" data-tab="signup" id="tabSignup">Sign Up</button>
    </div>

    <!-- LOGIN FORM -->
    <form class="auth-form" id="loginForm">
      <div class="field">
        <label for="login-user">Username</label>
        <input type="text" id="login-user" autocomplete="username" required>
      </div>
      <div class="field">
        <label for="login-pass">Password</label>
        <div class="pw-wrap">
          <input type="password" id="login-pass" autocomplete="current-password" required>
          <button type="button" class="pw-toggle" data-target="login-pass" aria-label="Show password" title="Show password">
            <svg class="icon-eye" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7Z"/><circle cx="12" cy="12" r="3"/></svg>
            <svg class="icon-eye-off" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" hidden><path d="M17.94 17.94A10.94 10.94 0 0 1 12 20c-7 0-11-7-11-7a20.6 20.6 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 7 11 7a20.6 20.6 0 0 1-2.16 3.19M14.12 14.12a3 3 0 1 1-4.24-4.24"/><path d="M1 1l22 22"/></svg>
          </button>
        </div>
      </div>
      <div style="text-align: right; margin-top: -6px; margin-bottom: 2px;">
        <button type="button" id="forgotPasswordBtn" style="background:none; border:none; padding:0; font-family:var(--font-mono); font-size:11px; color:var(--brass); cursor:pointer; text-decoration:underline;">Forgot Password?</button>
      </div>
      <div class="auth-error" id="loginError"></div>
      <button type="submit" class="auth-submit">Log In</button>
    </form>

    <!-- SIGN UP FORM -->
    <form class="auth-form" id="signupForm" hidden>
      <div class="name-row">
        <div class="field">
          <label for="signup-first">First Name</label>
          <input type="text" id="signup-first" autocomplete="given-name" required>
        </div>
        <div class="field">
          <label for="signup-last">Last Name</label>
          <input type="text" id="signup-last" autocomplete="family-name" required>
        </div>
      </div>
      <div class="field">
        <label for="signup-email">Email</label>
        <input type="email" id="signup-email" autocomplete="email" required>
      </div>
      <div class="field">
        <label for="signup-phone">Phone Number</label>
        <input type="tel" id="signup-phone" autocomplete="tel" placeholder="Optional">
      </div>
      <div class="field">
        <label for="signup-user">Username</label>
        <input type="text" id="signup-user" autocomplete="username" required>
      </div>
      <div class="field">
        <label for="signup-pass">Password</label>
        <div class="pw-wrap">
          <input type="password" id="signup-pass" autocomplete="new-password" required minlength="4">
          <button type="button" class="pw-toggle" data-target="signup-pass" aria-label="Show password" title="Show password">
            <svg class="icon-eye" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7Z"/><circle cx="12" cy="12" r="3"/></svg>
            <svg class="icon-eye-off" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" hidden><path d="M17.94 17.94A10.94 10.94 0 0 1 12 20c-7 0-11-7-11-7a20.6 20.6 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 7 11 7a20.6 20.6 0 0 1-2.16 3.19M14.12 14.12a3 3 0 1 1-4.24-4.24"/><path d="M1 1l22 22"/></svg>
          </button>
        </div>
        <span class="field-hint">At least 4 characters.</span>
      </div>
      <div class="field">
        <label for="signup-pass2">Confirm Password</label>
        <div class="pw-wrap">
          <input type="password" id="signup-pass2" autocomplete="new-password" required minlength="4">
          <button type="button" class="pw-toggle" data-target="signup-pass2" aria-label="Show password" title="Show password">
            <svg class="icon-eye" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7Z"/><circle cx="12" cy="12" r="3"/></svg>
            <svg class="icon-eye-off" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" hidden><path d="M17.94 17.94A10.94 10.94 0 0 1 12 20c-7 0-11-7-11-7a20.6 20.6 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 7 11 7a20.6 20.6 0 0 1-2.16 3.19M14.12 14.12a3 3 0 1 1-4.24-4.24"/><path d="M1 1l22 22"/></svg>
          </button>
        </div>
      </div>
      <div class="auth-error" id="signupError"></div>
      <button type="submit" class="auth-submit">Create Account</button>
    </form>

    <p class="auth-note">Accounts are securely managed by your backend database now.</p>
  </div>
</div>

<!-- ================= RESET PASSWORD SCREEN ================= -->
<div class="auth-screen" id="resetScreen" hidden>
  <div class="auth-card">
    <div class="auth-eyebrow">Security</div>
    <h1 class="auth-title">Set New <em>Password</em></h1>
    <p class="auth-sub">Choose a secure new password for your account.</p>

    <form class="auth-form" id="resetForm">
      <div class="field">
        <label for="new-pass">New Password</label>
        <div class="pw-wrap">
          <input type="password" id="new-pass" required minlength="4">
        </div>
        <span class="field-hint">At least 4 characters.</span>
      </div>
      <div class="field">
        <label for="new-pass-confirm">Confirm New Password</label>
        <div class="pw-wrap">
          <input type="password" id="new-pass-confirm" required minlength="4">
        </div>
      </div>
      <div class="auth-error" id="resetError"></div>
      <button type="submit" class="auth-submit">Update Password</button>
    </form>
  </div>
</div>

<!-- ================= APP ================= -->
<div id="appScreen" hidden>
<div class="shell">

  <!-- Global Feature Paywall Modal -->
  <div id="globalPaywallModal" hidden style="position:fixed; inset:0; background:rgba(34,40,31,0.85); display:flex; flex-direction:column; align-items:center; justify-content:center; text-align:center; z-index:1000; padding:20px;">
      <div class="panel" style="max-width:400px; width:100%; border:2px solid var(--brass);">
          <h3 style="font-family:var(--font-display); color:var(--green-deep); margin:0 0 8px; font-size:22px;">🔒 Pro Feature Locked</h3>
          <p style="font-size:13px; color:var(--ink-soft); margin:0 0 16px;">Scan the QR code to upgrade to Premium (₹99) via UPI. Access unlocks automatically upon payment verification.</p>
          <img src="https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=upi://pay?pa=atharvarengade94@okhdfcbank%26pn=Atharva%20Rengade%26am=99.00%26cu=INR" alt="UPI QR Code" style="width:130px; height:130px; border:4px solid white; border-radius:4px; box-shadow:0 4px 10px rgba(0,0,0,0.15); margin-bottom:16px;">
          <div style="display:flex; justify-content:center; gap:10px;">
              <button type="button" id="verifyUpiBtn" style="background:var(--green-deep); color:var(--card-bg); border:none; padding:10px 16px; font-family:var(--font-mono); font-size:10px; cursor:pointer; border-radius:2px; text-transform:uppercase;">I Have Paid</button>
              <button type="button" id="closeGlobalPaywall" style="background:none; border:1px solid var(--rule-strong); padding:10px 20px; font-family:var(--font-mono); font-size:10px; cursor:pointer; border-radius:2px; text-transform:uppercase;">Close</button>
          </div>
      </div>
  </div>

  <div class="topbar">
    <div class="brand">
      <span class="eyebrow">Personal Accounts</span>
      <h1>Track Your Expense Monthly</h1>
    </div>
    <div class="account">
      <span>Signed in as <span class="who" id="whoAmI"></span></span>
      <button class="logout-btn" id="logoutBtn">Log Out</button>
      <button class="logout-btn" style="border-color: #dc3545; color: #dc3545;" id="deleteAccountBtn">Delete Account</button>
    </div>
  </div>

  <nav class="nav-tabs" id="navTabs">
    <button data-view="dashboard" class="active"><span class="dot"></span>Dashboard</button>
    <button data-view="new"><span class="dot"></span>New Entry</button>
    <button data-view="ledger"><span class="dot"></span>Ledger</button>
    <button data-view="analysis"><span class="dot"></span>Analysis</button>
    <button data-view="wealth"><span class="dot"></span>Wealth</button>
    <button data-view="summary"><span class="dot"></span>Summary</button>
  </nav>

  <!-- ---------- DASHBOARD ---------- -->
  <section class="view" id="view-dashboard">
    <h2 class="view-title">Welcome back</h2>
    <p class="view-sub">Your personal money overview, all in one place.</p>

    <div class="card-grid">
      <div class="stat-card">
        <div class="lbl">Net Balance</div>
        <div class="val" id="dashBalance">₹0.00</div>
        <div class="note">Income minus expenses this period</div>
      </div>
      <div class="stat-card">
        <div class="lbl">Total Income</div>
        <div class="val" id="dashIncome">₹0.00</div>
        <div class="stat-bar"><div style="width:100%"></div></div>
      </div>
      <div class="stat-card">
        <div class="lbl">Total Expenses</div>
        <div class="val red" id="dashExpense">₹0.00</div>
        <div class="note" id="dashExpensePct">0% of income spent</div>
      </div>
      <div class="stat-card">
        <div class="lbl">Transactions</div>
        <div class="val" id="dashCount">0</div>
        <div class="note">Entries in your ledger</div>
      </div>
    </div>

    <!-- Category Budgets Widget -->
    <div class="panel" style="margin-top:24px;">
      <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:14px;">
        <h3 style="font-family:var(--font-display); font-size:18px; color:var(--green-deep); margin:0;">Monthly Category Budgets</h3>
        <button type="button" id="openBudgetModal" style="background:var(--brass); color:var(--card-bg); border:none; padding:6px 12px; font-family:var(--font-mono); font-size:10px; text-transform:uppercase; cursor:pointer; border-radius:2px;">Set Limits</button>
      </div>
      <div id="budgetProgressList" style="display:flex; flex-direction:column; gap:12px;">
        <p style="font-size:12px; color:var(--ink-soft); margin:0;">No budget caps configured yet. Click "Set Limits" to track categories.</p>
      </div>
    </div>

    <!-- Hidden Budget Modal / Form Panel -->
    <div id="budgetModal" hidden style="position:fixed; inset:0; background:transparent; display:flex; align-items:center; justify-content:center; z-index:100; padding:16px;">
      <div class="panel" style="width:100%; max-width:380px; background:var(--card-bg); border:1px solid var(--rule-strong); border-radius:4px; padding:24px; box-shadow:0 10px 25px rgba(0,0,0,0.15);">
        <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:16px;">
          <h3 style="font-family:var(--font-display); font-size:20px; color:var(--green-deep); margin:0;">Configure Limit</h3>
          <button type="button" id="closeBudgetModalTop" style="background:none; border:none; font-family:var(--font-mono); font-size:14px; cursor:pointer; color:var(--ink-soft);">✕</button>
        </div>
        <form id="budgetForm" style="display:flex; flex-direction:column; gap:16px;">
          <div class="field" style="display:flex; flex-direction:column; gap:6px;">
            <label for="b-cat" style="font-family:var(--font-mono); font-size:10px; letter-spacing:.12em; text-transform:uppercase; color:var(--ink-soft);">Category</label>
            <select id="b-cat" style="width:100%; background:var(--paper-white); border:1px solid var(--rule-strong); border-radius:3px; padding:10px 12px; font-family:var(--font-mono); font-size:13px; color:var(--ink);">
              <option value="Food">Food</option>
              <option value="Transport">Transport</option>
              <option value="Housing">Housing</option>
              <option value="Utilities">Utilities</option>
              <option value="Health">Health</option>
              <option value="Shopping">Shopping</option>
              <option value="Entertainment">Entertainment</option>
              <option value="Other">Other</option>
            </select>
          </div>
          <div class="field" style="display:flex; flex-direction:column; gap:6px;">
            <label for="b-limit" style="font-family:var(--font-mono); font-size:10px; letter-spacing:.12em; text-transform:uppercase; color:var(--ink-soft);">Monthly Limit (₹)</label>
            <input type="number" id="b-limit" min="1" step="1" placeholder="e.g. 5000" required style="width:100%; background:var(--paper-white); border:1px solid var(--rule-strong); border-radius:3px; padding:10px 12px; font-family:var(--font-mono); font-size:13px; color:var(--ink);">
          </div>
          <div style="display:flex; justify-content:flex-end; gap:12px; margin-top:8px;">
            <button type="button" id="closeBudgetModal" style="background:transparent; border:1px solid var(--rule-strong); padding:10px 16px; font-family:var(--font-mono); font-size:10px; letter-spacing:.1em; text-transform:uppercase; cursor:pointer; border-radius:3px; color:var(--ink-soft);">Cancel</button>
            <button type="submit" class="submit-btn" style="padding:10px 20px; border-radius:3px;">Save Limit</button>
          </div>
        </form>
      </div>
    </div>

    <div class="recent-wrap">
      <div class="recent-head">
        <span class="lbl">Recent Transactions</span>
        <button type="button" id="viewAllBtn">View All →</button>
      </div>
      <div id="recentList"></div>
    </div>
  </section>

  <!-- ---------- NEW ENTRY ---------- -->
  <section class="view" id="view-new" hidden>
    <div class="panel">
      <div style="display:flex; justify-content:space-between; align-items:flex-start; margin-bottom:16px;">
        <div>
          <h2 class="view-title" style="font-size:22px;">New Entry</h2>
          <p class="view-sub">Record honestly. Every entry matters.</p>
        </div>
        <div>
          <input type="file" id="receiptInput" accept="image/*" capture="environment" hidden>
          <button type="button" id="scanBtn" style="background:var(--brass); color:var(--card-bg); border:none; padding:8px 12px; font-family:var(--font-mono); font-size:10px; text-transform:uppercase; cursor:pointer; border-radius:2px; display:flex; align-items:center; gap:6px; transition: transform 0.2s, background 0.3s, opacity 0.3s;">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect><circle cx="8.5" cy="8.5" r="1.5"></circle><polyline points="21 15 16 10 5 21"></polyline></svg>
            Scan Receipt
          </button>
        </div>
      </div>
      <form class="entry-form" id="entryForm">
        <div class="field">
          <label for="f-date">Date</label>
          <input type="date" id="f-date" required>
        </div>
        <div class="field">
          <label for="f-type">Type</label>
          <select id="f-type">
            <option value="expense">Expense</option>
            <option value="income">Income</option>
          </select>
        </div>
        <div class="field">
          <label for="f-cat">Category</label>
          <select id="f-cat"></select>
        </div>
        <div class="field">
          <label for="f-amt">Amount (₹)</label>
          <input type="number" id="f-amt" min="0.01" step="0.01" placeholder="0.00" required>
        </div>
        <div class="field full">
          <label for="f-desc">Description</label>
          <textarea id="f-desc" rows="3" placeholder="What was this for?" required></textarea>
        </div>
        <div class="form-actions">
          <span class="form-msg" id="formMsg"></span>
          <button type="submit" class="submit-btn">Add to Ledger →</button>
        </div>
      </form>
    </div>
  </section>

  <!-- ---------- LEDGER ---------- -->
  <section class="view" id="view-ledger" hidden>
    <div class="ledger-panel">
      <div class="ledger-head">
        <div>
          <h3>Ledger Book</h3>
          <p id="ledgerCount">Chronological record · 0 entries</p>
        </div>
        <button type="button" id="exportBtn" style="background:transparent; color:var(--card-bg); border:1px solid rgba(244,238,224,0.5); padding:6px 12px; font-family:var(--font-mono); font-size:10px; text-transform:uppercase; cursor:pointer; border-radius:2px; transition: background 0.2s, color 0.2s;" onmouseover="this.style.background='var(--card-bg)'; this.style.color='var(--green-deep)';" onmouseout="this.style.background='transparent'; this.style.color='var(--card-bg)';">
            Export CSV
        </button>
      </div>
      <div style="overflow-x:auto;">
        <table class="ledger">
          <thead>
            <tr><th>Date</th><th>Type</th><th>Category</th><th>Amount</th><th>Description</th><th></th></tr>
          </thead>
          <tbody id="ledgerBody"></tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- ---------- ANALYSIS ---------- -->
  <section class="view" id="view-analysis" hidden>
    <h2 class="view-title">Analysis</h2>
    <p class="view-sub">Patterns from your handwritten ledger.</p>

    <div class="analysis-grid">
      <div class="panel">
        <span class="lbl" style="font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:var(--ink-soft);">By Category</span>
        <div class="chart-holder"><div id="catChart" style="width:100%;height:100%;"></div></div>
        <div class="legend-row" id="catLegend"></div>
      </div>
      <div class="panel">
        <span class="lbl" style="font-family:var(--font-mono); font-size:10px; letter-spacing:.15em; text-transform:uppercase; color:var(--ink-soft);">Monthly Trend</span>
        <div class="chart-holder"><div id="trendChart" style="width:100%;height:100%;"></div></div>
        <div class="legend-bars">
          <span><span class="swatch" style="background:var(--green-deep);"></span>Income</span>
          <span><span class="swatch" style="background:var(--red);"></span>Expense</span>
        </div>
      </div>
    </div>

    <div class="panel suggest-panel">
      <div class="head">
        <h3>Smart Suggestions</h3>
        <span class="placeholder-tag">Gemini AI</span>
      </div>
      <p class="suggest-intro">AI-powered insights based on your spending.</p>
      
      <!-- Boost Savings 3D Chart -->
      <div id="boostSavingsContainer" style="margin-top: 10px;"></div>

      <!-- Wrapper for overlay -->
      <div style="position:relative; min-height: 150px; margin-top: 16px;">
        <div class="suggest-list" id="suggestList" style="margin-top: 0;">
          <div class="suggest-item">
            <p><i>Analyzing your ledger with AI...</i></p>
          </div>
        </div>
        
        <!-- Locked Payment Overlay -->
        <div id="paywallOverlay" hidden style="position:absolute; inset:-10px; background:rgba(244,238,224,0.90); display:flex; flex-direction:column; align-items:center; justify-content:center; text-align:center; border-radius:3px; z-index:10; padding:20px;">
            <h4 style="font-family:var(--font-display); color:var(--green-deep); margin:0 0 8px; font-size:18px;">Unlock AI Insights</h4>
            <p style="font-size:12px; color:var(--ink-soft); margin:0 0 12px;">Scan the QR code to upgrade to Premium (₹99). Unlocks automatically once payment is verified.</p>
            <img src="https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=upi://pay?pa=atharvarengade94@okhdfcbank%26pn=Atharva%20Rengade%26am=99.00%26cu=INR" alt="UPI QR Code" style="width:120px; height:120px; border:4px solid white; border-radius:4px; box-shadow:0 4px 10px rgba(0,0,0,0.15); margin-bottom:12px;">
            <button type="button" class="verifyUpiBtnInner" style="background:var(--green-deep); color:var(--card-bg); border:none; padding:8px 14px; font-family:var(--font-mono); font-size:10px; cursor:pointer; border-radius:2px; text-transform:uppercase;">I Have Paid</button>
        </div>
      </div>

      <p class="suggest-foot">Educational only, not financial advice.</p>
    </div>
  </section>

  <!-- ---------- WEALTH / INVESTMENTS ---------- -->
  <section class="view" id="view-wealth" hidden>
    <h2 class="view-title">Wealth Portfolio</h2>
    <p class="view-sub">Track your long-term assets and total net worth.</p>

    <div class="card-grid" style="margin-top:22px;">
      <div class="stat-card">
        <div class="lbl">Total Invested</div>
        <div class="val" id="wealthInvested">₹0.00</div>
        <div class="note">Principal capital deployed</div>
      </div>
      <div class="stat-card">
        <div class="lbl">Current Portfolio Value</div>
        <div class="val" id="wealthCurrent" style="color:var(--green-deep);">₹0.00</div>
        <div class="note" id="wealthReturnNote">0.0% overall return</div>
      </div>
    </div>

    <div class="panel" style="margin-top:24px;">
      <h3 style="font-family:var(--font-display); font-size:18px; color:var(--green-deep); margin:0 0 14px;">Add Asset</h3>
      <form class="entry-form" id="investmentForm" style="margin-top:0;">
        <div class="field">
          <label for="inv-name">Asset Name</label>
          <input type="text" id="inv-name" placeholder="e.g. Reliance, Nifty 50 Index" required>
        </div>
        <div class="field">
          <label for="inv-type">Asset Type</label>
          <select id="inv-type">
            <option value="Stock">Stock</option>
            <option value="Mutual Fund">Mutual Fund / SIP</option>
            <option value="Crypto">Crypto</option>
            <option value="Gold">Gold</option>
            <option value="Real Estate">Real Estate</option>
            <option value="Other">Other</option>
          </select>
        </div>
        <div class="field">
          <label for="inv-invested">Invested Amount (₹)</label>
          <input type="number" id="inv-invested" min="0.01" step="0.01" placeholder="0.00" required>
        </div>
        <div class="field">
          <label for="inv-current">Current Value (₹)</label>
          <input type="number" id="inv-current" min="0.01" step="0.01" placeholder="0.00" required>
        </div>
        <div class="field">
          <label for="inv-date">Date</label>
          <input type="date" id="inv-date" required>
        </div>
        <div class="form-actions" style="grid-column: span 1;">
          <button type="submit" class="submit-btn" style="width:100%;">Add Asset →</button>
        </div>
      </form>
    </div>

    <div class="ledger-panel" style="margin-top:24px;">
      <div class="ledger-head">
        <div>
          <h3>Asset Allocation</h3>
          <p id="wealthCount">Portfolio record · 0 assets</p>
        </div>
      </div>
      <div style="overflow-x:auto;">
        <table class="ledger">
          <thead>
            <tr><th>Date</th><th>Asset Name</th><th>Type</th><th>Invested</th><th>Current Value</th><th>Returns</th><th></th></tr>
          </thead>
          <tbody id="wealthBody"></tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- ---------- SUMMARY ---------- -->
  <section class="view" id="view-summary" hidden>
    <h2 class="view-title">Summary</h2>
    <p class="view-sub">Reconciled totals from your ledger.</p>

    <div class="summary-grid">
      <div class="summary-left">
        <div class="summary-card">
          <div class="lbl">Total Income</div>
          <div class="val" id="sumIncome">₹0.00</div>
          <div class="sub" id="sumIncomeCount">0 income entries</div>
        </div>
        <div class="summary-card">
          <div class="lbl">Total Expenses</div>
          <div class="val red" id="sumExpense">₹0.00</div>
          <div class="sub" id="sumExpenseCount">0 expense entries</div>
        </div>
        <div class="savings-banner">
          <div>
            <div class="lbl">Net Savings</div>
            <div class="val" id="sumBalance">₹0.00</div>
          </div>
          <div>
            <div class="r-italic">Honestly accounted</div>
            <div class="r-sub" id="sumTotalCount">0 total records</div>
          </div>
        </div>
      </div>
      <div class="gauge-card">
        <div class="lbl">Saving Rate</div>
        <div class="gauge-wrap">
          <svg viewBox="0 0 140 140">
            <circle cx="70" cy="70" r="60" fill="none" stroke="#e2d8c1" stroke-width="10"></circle>
            <circle id="gaugeArc" cx="70" cy="70" r="60" fill="none" stroke="#203a24" stroke-width="10" stroke-linecap="round" style="transition: stroke-dashoffset 1.5s ease-out;"></circle>
          </svg>
          <div class="gauge-center">
            <span class="pct" id="gaugePct">0%</span>
            <span class="tag">saved</span>
          </div>
        </div>
        <p class="desc" id="gaugeDesc">You saved 0% of your income this period. Keep the ledger clean.</p>
      </div>
    </div>
  </section>

  <footer class="tagline"><p>Paper · Brass · Ink · Honest Accounts</p></footer>

</div>
</div>

<script>
(function(){
 const API_URL = "https://expense-tracker-er5c.onrender.com";
  const SESSION_KEY = 'tracker:session';

  const EXPENSE_CATS = ['Food','Transport','Housing','Utilities','Health','Shopping','Entertainment','Other'];
  const INCOME_CATS = ['Salary','Freelance','Investment','Gift','Other'];
  const CAT_COLORS = {
    Food:'#a13d2b', Transport:'#a9814a', Housing:'#2f5233', Utilities:'#536b8a',
    Health:'#7d6a52', Shopping:'#b36b45', Entertainment:'#8b9a70', Other:'#7a7a65',
    Salary:'#2f5233', Freelance:'#cba86a', Investment:'#596b60', Gift:'#9a875e'
  };

  let currentUser = null;
  let entries = [];
  let investments = [];
  let budgets = {};

  const $ = id => document.getElementById(id);

  /* ---------------- AUTH & RESET TOKEN CHECK ---------------- */
  const authScreen = $('authScreen');
  const resetScreen = $('resetScreen');
  const appScreen = $('appScreen');
  const loginForm = $('loginForm');
  const signupForm = $('signupForm');
  const authSubText = $('authSubText');
  const tabLogin = $('tabLogin');
  const tabSignup = $('tabSignup');
  const whoAmI = $('whoAmI');
  const logoutBtn = $('logoutBtn');

  // Check URL query parameters for reset token
  const urlParams = new URLSearchParams(window.location.search);
  const resetToken = urlParams.get('token');
  if (resetToken) {
      if(authScreen) authScreen.hidden = true;
      if(resetScreen) resetScreen.hidden = false;
  }

  // Handle password reset submission
  const resetForm = $('resetForm');
  if(resetForm) {
      resetForm.addEventListener('submit', async (e) => {
          e.preventDefault();
          const p1 = $('new-pass').value;
          const p2 = $('new-pass-confirm').value;
          const errEl = $('resetError');

          if(p1 !== p2) {
              errEl.textContent = 'Passwords do not match.';
              return;
          }

          try {
              const res = await fetch(`${API_URL}/reset-password`, {
                  method: 'POST',
                  headers: { 'Content-Type': 'application/json' },
                  body: JSON.stringify({ token: resetToken, new_password: p1 })
              });
              const data = await res.json();
              if(res.ok) {
                  alert("Password successfully reset! Please log in with your new credentials.");
                  window.location.href = window.location.pathname; // Remove token query string
              } else {
                  errEl.textContent = data.detail || 'Failed to reset password.';
              }
          } catch(err) {
              errEl.textContent = 'Server connection failed.';
          }
      });
  }

  function showLogin(){
    tabLogin.classList.add('active'); tabSignup.classList.remove('active');
    loginForm.hidden = false; signupForm.hidden = true;
    authSubText.textContent = 'Sign in to open your book.';
    $('loginError').textContent = '';
  }
  function showSignup(){
    tabSignup.classList.add('active'); tabLogin.classList.remove('active');
    signupForm.hidden = false; loginForm.hidden = true;
    authSubText.textContent = 'A few details to open a new book.';
    $('signupError').textContent = '';
  }
  tabLogin.addEventListener('click', showLogin);
  tabSignup.addEventListener('click', showSignup);

  document.querySelectorAll('.pw-toggle').forEach(btn => {
    btn.addEventListener('click', () => {
      const input = $(btn.dataset.target);
      const showing = input.type === 'text';
      input.type = showing ? 'password' : 'text';
      btn.querySelector('.icon-eye').hidden = !showing;
      btn.querySelector('.icon-eye-off').hidden = showing;
      btn.setAttribute('aria-label', showing ? 'Show password' : 'Hide password');
      btn.title = showing ? 'Show password' : 'Hide password';
    });
  });

  async function getSession(){
    return localStorage.getItem(SESSION_KEY);
  }
  
  async function setSession(username){
    if(username) localStorage.setItem(SESSION_KEY, username);
    else localStorage.removeItem(SESSION_KEY);
  }

  loginForm.addEventListener('submit', async (e) => {
    e.preventDefault();
    const uname = $('login-user').value.trim();
    const pass = $('login-pass').value;
    const errEl = $('loginError');
    if(!uname || !pass){ errEl.textContent = 'Enter both username and password.'; return; }

    try {
      const res = await fetch(`${API_URL}/login`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ username: uname, password: pass })
      });
      if(res.ok) {
        errEl.textContent = '';
        await enterApp(uname);
      } else {
        errEl.textContent = 'Incorrect username or password.';
      }
    } catch(err) {
      errEl.textContent = 'Server connection failed.';
    }
  });

  // Forgot Password Trigger Handler
  $('forgotPasswordBtn').addEventListener('click', async () => {
    const email = prompt("Enter your registered email address to receive a password reset link:");
    if (!email) return;

    try {
      const res = await fetch(`${API_URL}/forgot-password`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email: email.trim() })
      });
      const data = await res.json();
      if (res.ok) {
        alert("Success! A password reset link has been emailed to you.");
      } else {
        alert("Error: " + (data.detail || "Email address not found."));
      }
    } catch (err) {
      alert("Server connection failed.");
    }
  });

  signupForm.addEventListener('submit', async (e) => {
    e.preventDefault();
    const first = $('signup-first').value.trim();
    const last = $('signup-last').value.trim();
    const email = $('signup-email').value.trim();
    const phone = $('signup-phone').value.trim();
    const uname = $('signup-user').value.trim();
    const pass = $('signup-pass').value;
    const pass2 = $('signup-pass2').value;
    const errEl = $('signupError');

    if(!first || !last || !email || !uname || !pass || !pass2){ errEl.textContent = 'Please fill in all required fields.'; return; }
    const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if(!emailPattern.test(email)){ errEl.textContent = 'Enter a valid email address.'; return; }
    if(pass.length < 4){ errEl.textContent = 'Password must be at least 4 characters.'; return; }
    if(pass !== pass2){ errEl.textContent = 'Passwords do not match.'; return; }

    try {
      const res = await fetch(`${API_URL}/signup`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ 
          username: uname, password: pass, firstName: first, 
          lastName: last, email: email, phone: phone 
        })
      });
      if(res.ok) {
        errEl.textContent = '';
        await enterApp(uname);
      } else {
        errEl.textContent = 'Username is already taken or server error.';
      }
    } catch(err) {
      errEl.textContent = 'Server connection failed.';
    }
  });

  logoutBtn.addEventListener('click', async () => {
    await setSession(null);
    currentUser = null;
    entries = [];
    investments = [];
    budgets = {};
    appScreen.hidden = true;
    authScreen.hidden = false;
    loginForm.reset();
    signupForm.reset();
    showLogin();
  });

  // Delete Account Handler
  $('deleteAccountBtn').addEventListener('click', async () => {
    const confirmed = confirm("Are you sure you want to delete your account? This action is permanent and will erase all your entries, budgets, and investments.");
    if (!confirmed) return;

    try {
      const res = await fetch(`${API_URL}/users/${currentUser}`, {
        method: 'DELETE'
      });

      if (res.ok) {
        alert("Your account has been successfully deleted.");
        await setSession(null);
        currentUser = null;
        entries = [];
        investments = [];
        budgets = {};
        appScreen.hidden = true;
        authScreen.hidden = false;
        loginForm.reset();
        signupForm.reset();
        showLogin();
      } else {
        const data = await res.json();
        alert("Failed to delete account: " + (data.detail || "Unknown error"));
      }
    } catch (err) {
      console.error("Error deleting account:", err);
      alert("Server connection failed.");
    }
  });

  async function enterApp(username){
    currentUser = username;
    await setSession(username);
    whoAmI.textContent = username; 
    authScreen.hidden = true;
    if(resetScreen) resetScreen.hidden = true;
    appScreen.hidden = false;
    await loadEntries();
    await loadInvestments();
    await loadBudgets();
    switchView('dashboard');
  }

  /* ---------------- NAV ---------------- */
  const navTabs = $('navTabs');
  const views = ['dashboard','new','ledger','analysis','wealth','summary'];
  
  navTabs.addEventListener('click', async (e) => {
    const btn = e.target.closest('button');
    if(!btn) return;
    
    const targetView = btn.dataset.view;
    if(targetView === 'wealth') {
        const isPrem = await checkUserIsPremium();
        if(!isPrem) {
            $('globalPaywallModal').hidden = false;
            return;
        }
    }
    switchView(targetView);
  });
  
  $('viewAllBtn').addEventListener('click', () => switchView('ledger'));

  function switchView(view){
    views.forEach(v => {
      $(`view-${v}`).hidden = v !== view;
    });
    [...navTabs.children].forEach(b => b.classList.toggle('active', b.dataset.view === view));
    
    if(view === 'analysis') {
      renderCharts();
      renderBoostChart(); 
      loadAIInsights();   
    }
    if(view === 'wealth') {
      loadInvestments();
    }
  }

  async function checkUserIsPremium() {
      try {
          const res = await fetch(`${API_URL}/insights/${currentUser}`);
          if(res.ok) {
              const data = await res.json();
              return !data.locked;
          }
      } catch(e) {}
      return false;
  }

  // --- Manual UPI Verification Trigger ---
  async function handleUpiVerification() {
      try {
          const res = await fetch(`${API_URL}/verify-upi-payment`, {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({ username: currentUser, transaction_id: "UPI_MANUAL" })
          });
          if(res.ok) {
              alert("Payment verified successfully! Pro features unlocked.");
              $('globalPaywallModal').hidden = true;
              loadAIInsights();
              switchView('dashboard');
          } else {
              alert("Verification failed. Please try again.");
          }
      } catch(err) {
          alert("Server connection failed during verification.");
      }
  }

  $('verifyUpiBtn').addEventListener('click', handleUpiVerification);
  document.querySelectorAll('.verifyUpiBtnInner').forEach(el => {
      el.addEventListener('click', handleUpiVerification);
  });

  // --- 3D Boost Savings Chart Logic ---
  function renderBoostChart() {
    const t = totals();
    const container = $('boostSavingsContainer');
    
    if (t.income <= 0 || t.expense <= 0) {
       container.innerHTML = '';
       return;
    }
    
    const currentSavings = Math.max(0, t.balance);
    const possibleSavings = Math.max(0, t.income - (t.expense * 0.85)); 
    
    const maxH = 100;
    const curH = possibleSavings > 0 ? (currentSavings / possibleSavings) * maxH : 10;
    const posH = maxH;
    const extra = possibleSavings - currentSavings;

    container.innerHTML = `
      <div style="text-align:center; margin-bottom:20px; animation: fadeSlideUp 0.8s ease-out;">
         <h4 style="margin:0; font-family:var(--font-display); color:var(--green-deep); font-size:16px;">Boost Your Savings</h4>
         <p style="margin:4px 0 0; font-size:12px; color:var(--ink-soft);">If you cut non-essential expenses by 15%, you could save an extra <strong style="color:var(--green-deep);">+${fmt(extra)}</strong></p>
      </div>
      <div class="chart-3d-container">
         <div class="bar-3d-wrapper">
            <span class="bar-val">${fmt(currentSavings)}</span>
            <div class="bar-3d" id="curBar3d" style="height:0px;"></div>
            <span class="bar-lbl">Current</span>
         </div>
         <div class="bar-3d-wrapper">
            <span class="bar-val" style="color:var(--brass); font-weight:bold;">${fmt(possibleSavings)}</span>
            <div class="bar-3d boost" id="posBar3d" style="height:0px;"></div>
            <span class="bar-lbl">Boosted</span>
         </div>
      </div>
    `;

    setTimeout(() => {
        const cur = document.getElementById('curBar3d');
        const pos = document.getElementById('posBar3d');
        if(cur) cur.style.height = `${Math.max(curH, 5)}px`;
        if(pos) pos.style.height = `${posH}px`;
    }, 100);
  }

  // --- AI Insights and Payment Status Check ---
  async function loadAIInsights() {
    const list = document.getElementById('suggestList');
    const paywall = document.getElementById('paywallOverlay');
    
    paywall.hidden = true;
    list.style.filter = 'none';
    list.innerHTML = '<div class="suggest-item"><p><i>Analyzing your ledger with AI...</i></p></div>';
    
    try {
      const res = await fetch(`${API_URL}/insights/${currentUser}`);
      if(res.ok) {
        const data = await res.json();
        
        if(data.locked) {
            list.innerHTML = '<div class="suggest-item"><p>Premium content locked.</p></div><div class="suggest-item"><p>Complete payment to see personalized tips.</p></div>';
            paywall.hidden = false;
        } else {
            list.innerHTML = data.html;
        }
      } else {
        list.innerHTML = '<div class="suggest-item"><p>AI server is running, but returned an error.</p></div>';
      }
    } catch(err) {
      list.innerHTML = '<div class="suggest-item"><p>Failed to reach AI server. Is Uvicorn running?</p></div>';
    }
  }

  /* ---------------- BUDGETS LOGIC ---------------- */
  const budgetModal = $('budgetModal');
  $('openBudgetModal').addEventListener('click', async () => {
      const isPrem = await checkUserIsPremium();
      if(!isPrem) {
          $('globalPaywallModal').hidden = false;
          return;
      }
      budgetModal.hidden = false;
  });
  $('closeBudgetModal').addEventListener('click', () => budgetModal.hidden = true);
  const closeTop = $('closeBudgetModalTop');
  if(closeTop) closeTop.addEventListener('click', () => budgetModal.hidden = true);

  async function loadBudgets(){
    try {
      const res = await fetch(`${API_URL}/budgets/${currentUser}`);
      if(res.ok) {
        budgets = await res.json();
      } else {
        budgets = {};
      }
    } catch(e) {
      budgets = {};
    }
    renderBudgets();
  }

  $('budgetForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const payload = {
      category: $('b-cat').value,
      monthly_limit: parseFloat($('b-limit').value)
    };

    try {
      const res = await fetch(`${API_URL}/budgets/${currentUser}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
      });
      if(res.ok) {
        budgetModal.hidden = true;
        $('b-limit').value = '';
        await loadBudgets();
      }
    } catch(err) {
      console.error('Failed to save budget limit');
    }
  });

  $('budgetProgressList').addEventListener('click', async (e) => {
    const btn = e.target.closest('.remove-budget-btn');
    if(!btn) return;
    
    const category = btn.dataset.category;
    try {
      const res = await fetch(`${API_URL}/budgets/${currentUser}/${category}`, {
        method: 'DELETE'
      });
      if(res.ok) {
        await loadBudgets();
      }
    } catch(err) {
      console.error('Failed to remove budget limit');
    }
  });

  function renderBudgets() {
    const container = $('budgetProgressList');
    const categories = Object.keys(budgets);

    if(categories.length === 0){
      container.innerHTML = '<p style="font-size:12px; color:var(--ink-soft); margin:0;">No budget caps configured yet. Click "Set Limits" to track categories.</p>';
      return;
    }

    const currentMonth = new Date().toISOString().slice(0, 7);
    const spentByCategory = {};
    
    entries.forEach(e => {
      if(e.type === 'expense' && e.date.startsWith(currentMonth)){
        spentByCategory[e.cat] = (spentByCategory[e.cat] || 0) + e.amount;
      }
    });

    container.innerHTML = categories.map(cat => {
      const limit = budgets[cat];
      const spent = spentByCategory[cat] || 0;
      const pct = Math.min(100, Math.round((spent / limit) * 100));
      
      let barColor = 'var(--green-deep)';
      if(pct >= 100) barColor = 'var(--red)';
      else if(pct >= 75) barColor = 'var(--brass)';

      return `
        <div style="background:var(--paper-white); border:1px solid rgba(199,187,156,0.6); border-radius:3px; padding:10px 14px;">
          <div style="display:flex; justify-content:space-between; align-items:center; font-family:var(--font-mono); font-size:11px; margin-bottom:6px;">
            <span style="font-weight:600; color:var(--ink);">${escapeHtml(cat)}</span>
            <div style="display:flex; align-items:center; gap:10px;">
              <span>${fmt(spent)} / ${fmt(limit)} (${pct}%)</span>
              <button class="remove-budget-btn" data-category="${escapeHtml(cat)}" style="background:none; border:none; cursor:pointer; font-family:var(--font-mono); font-size:9px; text-transform:uppercase; color:var(--red);">Remove</button>
            </div>
          </div>
          <div style="height:6px; width:100%; background:rgba(32,58,36,0.1); border-radius:3px; overflow:hidden;">
            <div style="height:100%; width:${pct}%; background:${barColor}; transition: width 0.6s ease-out, background 0.3s;"></div>
          </div>
        </div>
      `;
    }).join('');
  }

  /* ---------------- WEALTH / INVESTMENTS LOGIC ---------------- */
  $('inv-date').valueAsDate = new Date();

  async function loadInvestments(){
    try {
      const res = await fetch(`${API_URL}/investments/${currentUser}`);
      if(res.ok) {
        investments = await res.json();
      } else {
        investments = [];
      }
    } catch(e) { 
      investments = []; 
      console.error('Failed to fetch investments'); 
    }
    renderInvestments();
  }

  $('investmentForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const newInv = {
      asset_name: $('inv-name').value.trim(),
      asset_type: $('inv-type').value,
      invested_amount: parseFloat($('inv-invested').value),
      current_value: parseFloat($('inv-current').value),
      date: $('inv-date').value
    };

    try {
      await fetch(`${API_URL}/investments/${currentUser}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(newInv)
      });
      $('inv-name').value = '';
      $('inv-invested').value = '';
      $('inv-current').value = '';
      await loadInvestments();
    } catch(err) {
      console.error('Failed to save investment');
    }
  });

  document.getElementById('wealthBody').addEventListener('click', async (e) => {
    const btn = e.target.closest('.del-link');
    if(!btn) return;
    try {
      await fetch(`${API_URL}/investments/${currentUser}/${btn.dataset.id}`, { method: 'DELETE' });
      await loadInvestments(); 
    } catch(err) {
      console.error('Failed to delete investment');
    }
  });

  function renderInvestments() {
    const totalInvested = investments.reduce((sum, i) => sum + i.invested_amount, 0);
    const totalCurrent = investments.reduce((sum, i) => sum + i.current_value, 0);
    const totalProfit = totalCurrent - totalInvested;
    const returnPct = totalInvested > 0 ? ((totalProfit / totalInvested) * 100).toFixed(2) : 0;

    $('wealthInvested').textContent = fmt(totalInvested);
    $('wealthCurrent').textContent = fmt(totalCurrent);
    
    const noteEl = $('wealthReturnNote');
    noteEl.textContent = `${returnPct}% overall return (${fmt(totalProfit)})`;
    noteEl.style.color = totalProfit >= 0 ? 'var(--green-deep)' : 'var(--red)';

    $('wealthCount').textContent = `Portfolio record · ${investments.length} ${investments.length === 1 ? 'asset' : 'assets'}`;
    
    const body = $('wealthBody');
    if(investments.length === 0){
      body.innerHTML = '<tr><td colspan="7" style="text-align:center; padding:40px 16px; font-family:var(--font-body); color:var(--ink-soft);">No assets tracked yet. Add your first investment above.</td></tr>';
    } else {
      body.innerHTML = investments.map(inv => {
        const profit = inv.current_value - inv.invested_amount;
        const pPct = inv.invested_amount > 0 ? ((profit / inv.invested_amount) * 100).toFixed(1) : 0;
        const colorClass = profit >= 0 ? 'income' : 'expense';
        return `
          <tr>
            <td style="font-family:var(--font-mono); font-size:12px;">${inv.date}</td>
            <td style="font-weight:500;">${escapeHtml(inv.asset_name)}</td>
            <td><span class="type-pill" style="background:var(--tan); color:var(--ink); border-color:var(--rule);">${escapeHtml(inv.asset_type)}</span></td>
            <td style="font-family:var(--font-mono);">${fmt(inv.invested_amount)}</td>
            <td style="font-family:var(--font-mono);">${fmt(inv.current_value)}</td>
            <td class="amt ${colorClass}">${profit >= 0 ? '+' : ''}${fmt(profit)} (${pPct}%)</td>
            <td><button class="del-link" data-id="${inv.id}">Delete</button></td>
          </tr>
        `;
      }).join('');
    }
  }


  /* ---------------- CATEGORY <-> TYPE ---------------- */
  const fType = $('f-type');
  const fCat = $('f-cat');
  function populateCategoryOptions(){
    const list = fType.value === 'income' ? INCOME_CATS : EXPENSE_CATS;
    fCat.innerHTML = list.map(c => `<option value="${c}">${c}</option>`).join('');
  }
  fType.addEventListener('change', populateCategoryOptions);
  populateCategoryOptions();
  $('f-date').valueAsDate = new Date();

  function fmt(n){
    const sign = n < 0 ? '-' : '';
    return sign + '₹' + Math.abs(n).toLocaleString('en-IN', {minimumFractionDigits:2, maximumFractionDigits:2});
  }
  function escapeHtml(s){
    return s.replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  }

  /* ---------------- DATA ---------------- */
  async function loadEntries(){
    try {
      const res = await fetch(`${API_URL}/entries/${currentUser}`);
      if(res.ok) {
        entries = await res.json();
      } else {
        entries = [];
      }
    } catch(e) { 
      entries = []; 
      console.error('Failed to fetch ledger from server'); 
    }
    render();
  }

  $('entryForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const formMsg = $('formMsg');
    formMsg.textContent = '';
    const amount = parseFloat($('f-amt').value);
    const desc = $('f-desc').value.trim();
    
    if(!$('f-date').value || !desc || !amount || amount <= 0){
      formMsg.textContent = 'Please fill in date, description, and a valid amount.';
      return;
    }
    
    const newEntry = {
      date: $('f-date').value,
      type: fType.value,
      cat: fCat.value,
      amount: amount,
      desc: desc
    };

    try {
      await fetch(`${API_URL}/entries/${currentUser}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(newEntry)
      });

      $('f-desc').value = '';
      $('f-amt').value = '';
      await loadEntries();
      await loadBudgets();
      switchView('ledger');
    } catch(err) {
      formMsg.textContent = 'Failed to save entry to the server.';
    }
  });

  $('ledgerBody').addEventListener('click', async (e) => {
    const btn = e.target.closest('.del-link');
    if(!btn) return;

    try {
      await fetch(`${API_URL}/entries/${currentUser}/${btn.dataset.id}`, { method: 'DELETE' });
      await loadEntries(); 
      await loadBudgets();
    } catch(err) {
      console.error('Failed to delete entry');
    }
  });

  function totals(){
    const income = entries.filter(e => e.type==='income').reduce((s,e)=>s+e.amount,0);
    const expense = entries.filter(e => e.type==='expense').reduce((s,e)=>s+e.amount,0);
    return { income, expense, balance: income-expense, count: entries.length };
  }

  function render(){
    const t = totals();

    $('dashBalance').textContent = fmt(t.balance);
    $('dashIncome').textContent = fmt(t.income);
    $('dashExpense').textContent = fmt(t.expense);
    $('dashExpensePct').textContent = `${t.income > 0 ? Math.round(t.expense / t.income * 100) : 0}% of income spent`;
    $('dashCount').textContent = t.count;

    renderBudgets();

    const sorted = [...entries].sort((a,b) => b.date.localeCompare(a.date) || (b.id - a.id));  
    const recentList = $('recentList');
    if(sorted.length === 0){
      recentList.innerHTML = '<div class="empty-note">No entries yet. Add your first transaction.</div>';
    }else{
      recentList.innerHTML = sorted.slice(0,3).map(en => `
        <div class="recent-row">
          <div class="recent-left">
            <span class="bar-stripe ${en.type}"></span>
            <div>
              <div class="rt-cat">${escapeHtml(en.cat)}</div>
              <div class="rt-meta">${en.date} · ${escapeHtml(en.desc)}</div>
            </div>
          </div>
          <span class="recent-amt ${en.type}">${en.type === 'income' ? '+' : '-'}${fmt(en.amount)}</span>
        </div>
      `).join('');
    }

    $('ledgerCount').textContent = `Chronological record · ${entries.length} ${entries.length === 1 ? 'entry' : 'entries'}`;
    const ledgerBody = $('ledgerBody');
    if(sorted.length === 0){
      ledgerBody.innerHTML = '<tr><td colspan="6" style="text-align:center; padding:40px 16px; font-family:var(--font-body); color:var(--ink-soft);">No entries yet. Add your first transaction.</td></tr>';
    }else{
      ledgerBody.innerHTML = sorted.map(en => `
        <tr>
          <td style="font-family:var(--font-mono); font-size:12px;">${en.date}</td>
          <td><span class="type-pill ${en.type}">${en.type}</span></td>
          <td>${escapeHtml(en.cat)}</td>
          <td class="amt ${en.type}">${fmt(en.amount)}</td>
          <td class="desc-cell" title="${escapeHtml(en.desc)}">${escapeHtml(en.desc)}</td>
          <td><button class="del-link" data-id="${en.id}">Delete</button></td>
        </tr>
      `).join('');
    }

    $('sumIncome').textContent = fmt(t.income);
    $('sumIncomeCount').textContent = `${entries.filter(e=>e.type==='income').length} income entries`;
    $('sumExpense').textContent = fmt(t.expense);
    $('sumExpenseCount').textContent = `${entries.filter(e=>e.type==='expense').length} expense entries`;
    $('sumBalance').textContent = fmt(t.balance);
    $('sumTotalCount').textContent = `${t.count} total records`;

    const savingRate = t.income > 0 ? Math.max(0, Math.min(100, Math.round(t.balance / t.income * 100))) : 0;
    $('gaugePct').textContent = savingRate + '%';
    $('gaugeDesc').textContent = `You saved ${savingRate}% of your income this period. Keep the ledger clean.`;
    const circumference = 2 * Math.PI * 60;
    const gaugeArc = $('gaugeArc');
    gaugeArc.setAttribute('stroke-dasharray', `${circumference}`);
    gaugeArc.setAttribute('stroke-dashoffset', `${circumference * (1 - savingRate/100)}`);

    if(!$('view-analysis').hidden) {
        renderCharts();
        renderBoostChart(); 
    }
  }

  function monthLabel(m){
    const [y, mo] = m.split('-');
    const names = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
    return `${names[parseInt(mo,10)-1]} ${y}`;
  }

  function svgEmpty(title, message){
    return `<svg viewBox="0 0 400 260" width="100%" height="100%" xmlns="http://www.w3.org/2000/svg" role="img" preserveAspectRatio="xMidYMid meet">
      <text x="200" y="122" text-anchor="middle" font-family="Inter, sans-serif" font-size="14" fill="#4a5142">${escapeHtml(title)}</text>
      <text x="200" y="144" text-anchor="middle" font-family="Inter, sans-serif" font-size="12" fill="#8a8a78">${escapeHtml(message)}</text>
    </svg>`;
  }

  function svgDonut(labels, data, colors, total){
    if(!labels.length) return svgEmpty('Expenses by Category', 'Add an expense to see this chart.');
    const cx = 200, cy = 130, r = 80, strokeW = 28;
    const circumference = 2 * Math.PI * r;
    let offset = 0;
    let arcs = '';
    let textLabels = '';

    labels.forEach((label, i) => {
      const val = data[i];
      const pct = total ? val / total : 0;
      const dash = circumference * pct;
      
      arcs += `<circle cx="${cx}" cy="${cy}" r="${r}" fill="none" stroke="${colors[i]}" stroke-width="${strokeW}" 
               stroke-dashoffset="${(-offset).toFixed(2)}" transform="rotate(-90 ${cx} ${cy})" 
               style="stroke-dasharray: 0 2000; animation: drawArc 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards; animation-delay: ${(i * 0.1)}s; --target-dash: ${dash.toFixed(2)}; --target-gap: ${(circumference-dash).toFixed(2)};"/>`;
      
      if (pct > 0.03) {
        const midAngle = 2 * Math.PI * (offset + dash / 2) / circumference - Math.PI / 2;
        const labelR = r + 24;
        const lx = cx + labelR * Math.cos(midAngle);
        const ly = cy + labelR * Math.sin(midAngle);
        const anchor = lx < cx ? 'end' : (lx > cx ? 'start' : 'middle');
        
        textLabels += `<text x="${lx.toFixed(1)}" y="${ly.toFixed(1)}" text-anchor="${anchor}" font-family="'IBM Plex Mono', monospace" font-size="10" font-weight="600" fill="#22281f" style="opacity:0; animation: popText 0.5s ease-out forwards; animation-delay: ${(0.6 + i * 0.1)}s;">${escapeHtml(label)}: ${fmt(val)}</text>`;
      }

      offset += dash;
    });

    return `<svg viewBox="0 0 400 260" width="100%" height="100%" xmlns="http://www.w3.org/2000/svg" role="img" preserveAspectRatio="xMidYMid meet">
      <circle cx="${cx}" cy="${cy}" r="${r}" fill="none" stroke="#e7decb" stroke-width="${strokeW}"/>
      ${arcs}
      <circle cx="${cx}" cy="${cy}" r="${r - strokeW/2 - 4}" fill="#f4eee0"/>
      <g style="opacity: 0; animation: popText 0.6s ease-out forwards; animation-delay: 0.4s;">
        <text x="${cx}" y="${cy-6}" text-anchor="middle" font-family="'IBM Plex Mono', monospace" font-size="11" font-weight="700" fill="#2f5233">Total</text>
        <text x="${cx}" y="${cy+12}" text-anchor="middle" font-family="'IBM Plex Mono', monospace" font-size="12" font-weight="600" fill="#22281f">${escapeHtml(fmt(total))}</text>
      </g>
      ${textLabels}
    </svg>`;
  }

  function svgMonthlyBars(months, byMonth){
    if(!months.length) return svgEmpty('Income vs Expenses', 'Add transactions to see the monthly comparison.');
    const width = 400, height = 260;
    const left = 40, right = 384, top = 18, bottom = 210;
    const maxVal = Math.max(1, ...months.flatMap(m => [byMonth[m].income, byMonth[m].expense]));
    const plotH = bottom - top, plotW = right - left;
    const groupW = plotW / months.length;
    const barW = Math.min(22, groupW / 3);
    let bars = '', labels = '';
    months.forEach((m, i) => {
      const center = left + groupW * (i + 0.5);
      const incH = (byMonth[m].income / maxVal) * plotH;
      const expH = (byMonth[m].expense / maxVal) * plotH;
      const x1 = center - barW - 2;
      const x2 = center + 2;
      bars += `<rect x="${x1.toFixed(1)}" y="${(bottom-incH).toFixed(1)}" width="${barW.toFixed(1)}" height="${incH.toFixed(1)}" rx="2" fill="#203a24" 
               style="transform-origin: center ${bottom}px; animation: scaleYUp 0.6s cubic-bezier(0.2, 0.8, 0.2, 1) forwards; animation-delay: ${(i * 0.1)}s; transform: scaleY(0);"/>`;
      bars += `<rect x="${x2.toFixed(1)}" y="${(bottom-expH).toFixed(1)}" width="${barW.toFixed(1)}" height="${expH.toFixed(1)}" rx="2" fill="#a13d2b" 
               style="transform-origin: center ${bottom}px; animation: scaleYUp 0.6s cubic-bezier(0.2, 0.8, 0.2, 1) forwards; animation-delay: ${(i * 0.1 + 0.05)}s; transform: scaleY(0);"/>`;
      
      labels += `<text x="${center.toFixed(1)}" y="${bottom+18}" text-anchor="middle" font-family="'IBM Plex Mono', monospace" font-size="10" fill="#4a5142">${escapeHtml(monthLabel(m))}</text>`;
    });
    return `<svg viewBox="0 0 ${width} ${height}" width="100%" height="100%" xmlns="http://www.w3.org/2000/svg" role="img" preserveAspectRatio="xMidYMid meet">
      <line x1="${left}" y1="${top}" x2="${left}" y2="${bottom}" stroke="#bdb39e"/>
      <line x1="${left}" y1="${bottom}" x2="${right}" y2="${bottom}" stroke="#bdb39e"/>
      ${bars}
      ${labels}
    </svg>`;
  }

  function renderCharts(){
    const byCat = {};
    entries.filter(e => e.type === 'expense').forEach(e => { byCat[e.cat] = (byCat[e.cat]||0) + e.amount; });
    const catLabels = Object.keys(byCat).sort((a,b) => byCat[b]-byCat[a]);
    const catData = catLabels.map(c => byCat[c]);
    const catColors = catLabels.map(c => CAT_COLORS[c] || '#7a7a65');
    const catTotal = catData.reduce((a,b)=>a+b, 0);

    $('catChart').innerHTML = svgDonut(catLabels, catData, catColors, catTotal);
    $('catLegend').innerHTML = catLabels.map(c => `<span><span class="swatch" style="background:${CAT_COLORS[c]||'#7a7a65'}"></span>${escapeHtml(c)}</span>`).join('') || '<span>No expenses yet</span>';

    const byMonth = {};
    entries.forEach(e => {
      const m = e.date.slice(0,7);
      byMonth[m] = byMonth[m] || {income:0, expense:0};
      byMonth[m][e.type] += e.amount;
    });
    const months = Object.keys(byMonth).sort();

    $('trendChart').innerHTML = svgMonthlyBars(months, byMonth);
  }

  // --- AI Receipt Scanner Logic ---
  const scanBtn = $('scanBtn');
  const receiptInput = $('receiptInput');

  if(scanBtn && receiptInput) {
    scanBtn.addEventListener('click', async () => {
      const isPrem = await checkUserIsPremium();
      if(!isPrem) {
          $('globalPaywallModal').hidden = false;
          return;
      }
      receiptInput.click();
    });

    receiptInput.addEventListener('change', async (e) => {
      const file = e.target.files[0];
      if(!file) return;

      const originalHTML = scanBtn.innerHTML;
      scanBtn.innerHTML = 'Scanning...';
      scanBtn.style.opacity = '0.7';
      scanBtn.disabled = true;

      const formData = new FormData();
      formData.append('file', file);

      try {
        const res = await fetch(`${API_URL}/scan-receipt/${currentUser}`, {
          method: 'POST',
          body: formData
        });
        
        if(res.ok) {
          const data = await res.json();
          
          if(data.date) $('f-date').value = data.date;
          if(data.amount) $('f-amt').value = data.amount;
          if(data.desc) $('f-desc').value = data.desc;
          if(data.cat) {
              $('f-type').value = 'expense';
              populateCategoryOptions(); 
              $('f-cat').value = data.cat;
          }
          
          scanBtn.innerHTML = 'Success!';
          scanBtn.style.background = 'var(--green-deep)';
        } else {
          scanBtn.innerHTML = 'Failed';
          scanBtn.style.background = 'var(--red)';
        }
      } catch(err) {
        console.error(err);
        scanBtn.innerHTML = 'Error';
        scanBtn.style.background = 'var(--red)';
      } finally {
        setTimeout(() => {
            scanBtn.innerHTML = originalHTML;
            scanBtn.style.background = 'var(--brass)';
            scanBtn.style.opacity = '1';
            scanBtn.disabled = false;
            receiptInput.value = ''; 
        }, 2000);
      }
    });
  }

  // --- CSV Export Logic ---
  const exportBtn = $('exportBtn');
  if(exportBtn) {
      exportBtn.addEventListener('click', async () => {
          const isPrem = await checkUserIsPremium();
          if(!isPrem) {
              $('globalPaywallModal').hidden = false;
              return;
          }
          window.location.href = `${API_URL}/export/${currentUser}`;
      });
  }

  $('closeGlobalPaywall').addEventListener('click', () => $('globalPaywallModal').hidden = true);

  /* ---------------- BOOT ---------------- */
  (async function boot(){
    if(!resetToken) {
        const sessionUser = await getSession();
        if(sessionUser){
          await enterApp(sessionUser);
        }
    }
  })();
})();
</script>

</body>
</html>